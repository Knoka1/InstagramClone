import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  storyContainer: {
    marginHorizontal: 7,
    width: 60,
    alignItems: 'center',
  },
  storyText: {
    fontSize: 11,
    color: 'black',
    marginTop: 7,
  },
});
